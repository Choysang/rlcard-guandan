# Copy to `.env` and adjust if your backend is not on localhost:5000.
# In development the Vite dev server (5173) connects to the backend here.
# When the page is served by the backend itself, the same-origin address
# is used automatically and this value is ignored.
VITE_BACKEND_URL=http://localhost:5000
